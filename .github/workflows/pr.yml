# ------------------------------------------------------------------------------
# <auto-generated>
#
#     This code was generated.
#
#     - To turn off auto-generation set:
#
#         [GitHubActions (AutoGenerate = false)]
#
#     - To trigger manual generation invoke:
#
#         nuke --generate-configuration GitHubActions_pr --host GitHubActions
#
# </auto-generated>
# ------------------------------------------------------------------------------

name: pr

on:
  pull_request:
    branches:
      - develop

jobs:
  windows-latest:
    name: windows-latest
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - name: 'Cache: .nuke/temp, ~/.nuget/packages'
        uses: actions/cache@v6
        with:
          path: |
            .nuke/temp
            ~/.nuget/packages
          key: ${{ runner.os }}-${{ hashFiles('global.json', '**/*.csproj') }}
      - name: 'Run: Test, ReportCoverage, Pack'
        run: ./build.cmd Test ReportCoverage Pack
        env:
          CodecovToken: ${{ secrets.CODECOV_TOKEN }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - name: 'Publish: test-results'
        uses: actions/upload-artifact@v4
        with:
          name: test-results-windows-latest
          path: artifacts/test-results
      - name: 'Publish: coverage-report.zip'
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report-windows-latest.zip
          path: artifacts/reports/coverage-report.zip
      - name: 'Publish: packages'
        uses: actions/upload-artifact@v4
        with:
          name: packages-windows-latest
          path: artifacts/packages
  ubuntu-latest:
    name: ubuntu-latest
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - name: 'Cache: .nuke/temp, ~/.nuget/packages'
        uses: actions/cache@v6
        with:
          path: |
            .nuke/temp
            ~/.nuget/packages
          key: ${{ runner.os }}-${{ hashFiles('global.json', '**/*.csproj') }}
      - name: 'Run: Test, ReportCoverage, Pack'
        run: ./build.cmd Test ReportCoverage Pack
        env:
          CodecovToken: ${{ secrets.CODECOV_TOKEN }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - name: 'Publish: test-results'
        uses: actions/upload-artifact@v4
        with:
          name: test-results-ubuntu-latest
          path: artifacts/test-results
      - name: 'Publish: coverage-report.zip'
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report-ubuntu-latest.zip
          path: artifacts/reports/coverage-report.zip
      - name: 'Publish: packages'
        uses: actions/upload-artifact@v4
        with:
          name: packages-ubuntu-latest
          path: artifacts/packages
  macos-latest:
    name: macos-latest
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - name: 'Cache: .nuke/temp, ~/.nuget/packages'
        uses: actions/cache@v6
        with:
          path: |
            .nuke/temp
            ~/.nuget/packages
          key: ${{ runner.os }}-${{ hashFiles('global.json', '**/*.csproj') }}
      - name: 'Run: Test, ReportCoverage, Pack'
        run: ./build.cmd Test ReportCoverage Pack
        env:
          CodecovToken: ${{ secrets.CODECOV_TOKEN }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - name: 'Publish: test-results'
        uses: actions/upload-artifact@v4
        with:
          name: test-results-macos-latest
          path: artifacts/test-results
      - name: 'Publish: coverage-report.zip'
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report-macos-latest.zip
          path: artifacts/reports/coverage-report.zip
      - name: 'Publish: packages'
        uses: actions/upload-artifact@v4
        with:
          name: packages-macos-latest
          path: artifacts/packages
